var inStock = true;
var shipping = false;

// Get the HTML element via its id attribute
var eStock = document.getElementById('stock');
// Get and set the HTML element's class attribute
eStock.className = inStock;

var eShipping = document.getElementById('shipping');
eShipping.className = shipping;
